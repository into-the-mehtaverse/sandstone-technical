
Routes should be thin HTTP layer

All business logic should live in services.

Domain-specific schemas live in /services

API-contracts import domain specific schemas and live in models/schemas.

Everything should be modular
